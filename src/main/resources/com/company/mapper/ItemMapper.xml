<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.ItemMapper">

		<resultMap type="com.company.domain.ItemDTO" id="itemMap">
			<result property="itemcode" column="itemcode"/>
			<result property="itemtitle" column="itemtitle"/>
			<result property="itemprice" column="itemprice"/>
			<result property="supplier" column="supplier"/>
			<result property="itemsize" column="itemsize"/>
			<result property="color" column="color"/>
		</resultMap>

		<!-- 목록 조회 -->
		<select id="itemlist" resultMap="itemMap">
			select * from item
		</select>
		
		<!-- 등록 -->
		<insert id="itemInsert">
			insert into item(itemcode,itemtitle,itemprice,supplier,itemsize,color) 
			values(#{itemcode}, #{itemtitle}, #{itemprice}, #{supplier},#{itemsize},#{color})
		</insert>
		
		<!-- 하나 가져오기 -->
		<select id="itemget" resultMap="itemMap">
			select * from item where itemcode=#{itemcode}
		</select>

		<!-- 삭제하기 -->
		<delete id="remove">
			delete from item where itemcode = #{itemcode}
		</delete>
		
		<!-- 수정하기 -->
		<update id="update">
			update item set itemcode=#{itemcode},itemtitle=#{itemtitle},itemprice=#{itemprice},supplier=#{supplier},itemsize=#{itemsize},color=#{color}
			where itemcode=#{itemcode}
		</update>

</mapper>