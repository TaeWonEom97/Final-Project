<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.SellMapper">

	<resultMap type="com.company.domain.SellItemDTO" id="sellItemMap">
		<id property="sellid" column="sellid"/>
		<result property="sellnum" column="sellnum"/>
		<result property="selldate" column="selldate"/>
		<result property="sellcode" column="sellcode"/>
		<result property="selluser" column="selluser"/>
		
		<collection property="itemDto" resultMap="itemMap"/>
		<collection property="customerDto" resultMap="customerMap"/>
	</resultMap>
	
	<resultMap type="com.company.domain.ItemDTO" id="itemMap">
		<result property="itemcode" column="itemcode"/>
		<result property="itemtitle" column="itemtitle"/>
		<result property="itemprice" column="itemprice"/>
		<result property="supplier" column="supplier"/>
		<result property="itemsize" column="itemsize"/>
		<result property="color" column="color"/>
	</resultMap>
	
	<resultMap type="com.company.domain.CustomerDTO" id="customerMap">
		<result property="userid" column="userid"/>
		<result property="password" column="password"/>
		<result property="company" column="company"/>
	</resultMap>
	
	<!-- 판매 목록 조회 -->
	<select id="sellList" resultMap="sellItemMap">
		select * from sellitem left outer join item on sellcode = itemcode left outer join customer on selluser = userid
	</select>
	
	<!-- 판매 등록 -->
	<insert id="sellInsert">
		insert into sellitem(sellcode, sellnum, sellid, selluser) values(#{sellcode}, #{sellnum}, sellid.nextval, #{selluser})
	</insert>
	
	<!-- 하나 가져오기 -->
	<select id="sellGet" resultMap="sellItemMap">
		select * from sellitem left outer join item on sellcode = itemcode left outer join customer on selluser = userid where sellid = #{sellid}
	</select>
	
	<!-- 삭제하기 -->
	<delete id="remove">
		delete from sellitem where sellid = #{sellid}
	</delete>
	
	<!-- 수정하기 -->
	<update id="update">
		update sellitem set sellnum = #{sellnum} where sellid = #{sellid}
	</update>
</mapper>
